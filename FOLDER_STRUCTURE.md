# 项目文件夹结构说明

本文档说明项目中各个文件夹的用途和存储内容。

## 图片存储文件夹

### 1. `AIGC_graph/` - AIGC生成的图片

**用途**：存储由图片AIGC功能生成的图片文件

**存储内容**：
- 图片AIGC（Huoshan模型）生成的所有图片
- 包括连环画、漫画等AI生成的图像

**文件命名**：
- 递增数字命名（1.jpg, 2.jpg, 3.jpg, ...）
- 每次生成前自动检查当前最大序号，在此基础上递增

**数据库关联**：
- 图片元数据存储在 `AIGC_graph` 表中
- 包含文件名、存储路径、标签等信息

**访问路径**：
- 前端访问：`/AIGC_graph/{filename}`
- 后端存储：项目根目录下的 `AIGC_graph/` 文件夹

**代码位置**：
- `AIGC/aigc_api_server.py` 第175-178行：设置保存目录
- `AIGC/image_RAG.py`：实际保存图片的逻辑

---

### 2. `crawled_images/` - 爬虫抓取的图片

**用途**：存储由爬虫程序抓取的文化资源图片

**存储内容**：
- 从民族文化资源库和维基百科爬取的图片
- 经过过滤，只保留与文化相关的图片（过滤掉logo、图标、横幅、二维码等）

**文件命名**：
- 递增数字命名（1.jpg, 2.jpg, 3.jpg, ...）
- 每次爬取前自动检查当前最大序号，在此基础上递增

**数据库关联**：
- 图片信息存储在 `crawled_images` 表中
- 包含文件名、存储路径、标签、尺寸等信息

**访问路径**：
- 前端访问：`/api/images/crawled/{filename}`
- 后端存储：项目根目录下的 `crawled_images/` 文件夹

**代码位置**：
- `spider/minzu_festivals_spider.py`：民族文化资源库爬虫
- `spider/wikipedia_spider.py`：维基百科爬虫
- `AIGC/aigc_api_server.py` 第1504-1533行：提供图片访问接口

---

### 3. `avatars/` - 用户头像（当前未使用）

**状态**：文件夹存在但当前代码中未使用

**原设计用途**：
- 存储用户上传的头像文件
- 命名格式：`avatar_{user_id}_{original_filename}`

**实际情况**：
- 当前头像实际存储在 `FrontEnd/public/` 文件夹
- 命名格式：`{account}.jpg`（使用账号，如：12345678.jpg）
- 前端配置了 `/avatars` 代理，但后端未实现对应路由

**建议**：
- 如果不需要，可以删除此文件夹
- 或者保留作为未来扩展使用

---

### 4. `images/` - 通用图片（当前未使用）

**状态**：文件夹存在但当前代码中未使用

**可能用途**：
- 预留的通用图片存储文件夹
- 或者之前版本使用过，现在已废弃

**建议**：
- 如果不需要，可以删除此文件夹
- 或者保留作为未来扩展使用

---

### 5. `FrontEnd/public/` - 前端静态资源和用户头像

**用途**：
- 存储前端静态资源（如默认头像、favicon等）
- **实际存储用户上传的头像文件**

**存储内容**：
- `default.jpg`：默认头像（所有用户未上传头像时显示）
- `{account}.jpg`：用户上传的头像（使用账号，如：12345678.jpg）
- `favicon.ico`：网站图标
- `videos/`：轮播视频文件

**头像处理**：
- 用户上传的头像会自动压缩到200x200像素
- 统一保存为JPG格式
- 命名格式：`{account}.jpg`（使用账号，如：12345678.jpg）
- **注意**：头像文件名使用账号（account），而非昵称（nickname）

**访问路径**：
- 默认头像：`/default.jpg`
- 用户头像：`/{account}.jpg`（使用账号，如：/12345678.jpg）
- 通过Flask的静态文件服务提供

**代码位置**：
- `AIGC/aigc_api_server.py` 第328-339行：注册时保存头像
- `AIGC/aigc_api_server.py` 第680-726行：更换头像时保存
- `AIGC/aigc_api_server.py` 第2290-2309行：提供静态文件服务

---

## 其他文件夹

### `uploads/` - 用户上传的资源文件

**用途**：存储用户上传的文本或图片资源（待审核）

**存储内容**：
- 用户通过"用户上传"功能上传的文件
- 包括文本文件和图片文件

**数据库关联**：
- 资源信息存储在 `cultural_resources_from_user` 表中
- 审核通过后可能迁移到 `cultural_resources` 表

**代码位置**：
- `upload_handler.py`：处理用户上传

---

## 文件夹使用总结

| 文件夹 | 用途 | 状态 | 存储内容 |
|--------|------|------|----------|
| `AIGC_graph/` | AIGC生成的图片 | ✅ 使用中 | 图片AIGC生成的图像 |
| `crawled_images/` | 爬虫抓取的图片 | ✅ 使用中 | 爬虫抓取的文化资源图片 |
| `FrontEnd/public/` | 前端静态资源 | ✅ 使用中 | 默认头像、用户头像、favicon等 |
| `uploads/` | 用户上传资源 | ✅ 使用中 | 用户上传的待审核文件（文件名格式：用户名-日期-时间） |

---

## 建议

1. **文件夹清理**：
   - 定期清理 `AIGC_graph/` 和 `crawled_images/` 中的旧文件（如果需要）
   - 注意：删除文件时也要同步删除数据库中的记录

2. **文件命名规范**：
   - `uploads/` 文件夹中的文件使用格式：`用户名-日期-时间.扩展名`
   - 便于在标注任务中展示和按时间排序

