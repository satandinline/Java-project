================================================================================
公共文化资源管理系统 - 技术实现手册
================================================================================

版本：2.0
更新日期：2025年12月20日

重要更新：
- 后端已从Python Flask迁移到Java Spring Boot
- 数据库连接从PyMySQL改为JDBC
- 采用标准的MVC架构（Controller-Service-DAO）

================================================================================
一、目录
================================================================================

1. 系统架构
2. 技术栈
3. 数据库设计
4. 核心功能实现
5. API接口文档
6. 代码规范与最佳实践
7. 部署与运维

================================================================================

系统架构

整体架构

┌─────────────────┐
│   前端 (Vue.js) │
│   Port: 5173    │
└────────┬────────┘
         │ HTTP/API (代理)
         ▼
┌─────────────────┐
│ Java后端 (Spring Boot) │
│  Port: 8000     │
│  JDBC连接MySQL  │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌──────────┐
│ MySQL  │ │ Python服务│
│ 数据库 │ │ (可选，AIGC)│
│(JDBC)  │ │ Port:8001│
└────────┘ └──────────┘

说明：
- Java后端负责主要的API服务（认证、资源、统计等）
- Python服务可选，用于AIGC功能（AI生成）
- 数据库使用JDBC连接（替代PyMySQL）

模块划分

1. 前端模块 (`FrontEnd/`)
   - 用户界面组件
   - 路由管理
   - 状态管理

2. Java后端模块 (`backend/`)
   - Spring Boot应用主类
   - Controller层：RESTful API接口
   - Service层：业务逻辑处理
   - DAO层：数据访问（使用JDBC）
   - Entity层：实体类定义
   - Config层：配置类（数据源、Web配置等）

3. Python后端模块（可选，用于AIGC功能）
   - `AIGC/aigc_api_server.py`：AIGC API服务器
   - RAG检索增强生成
   - 图像生成
   - 多模态理解

4. 数据爬取模块 (`spider/`)
   - 民族文化资源库爬虫
   - 维基百科爬虫
   - 数据清洗与存储

5. 数据库模块 (`database_files/`)
   - 数据库初始化
   - 表结构定义
   - 数据迁移脚本

================================================================================

技术栈

Java后端技术

- Java 17: 开发语言
- Spring Boot 3.2.0: Web应用框架
- Spring JDBC: 数据库访问框架
- JDBC: MySQL数据库连接（替代PyMySQL）
- MySQL Connector/J: MySQL JDBC驱动
- Maven: 项目管理和构建工具

Python后端技术（可选，用于AIGC功能）

- Python 3.7+
- Flask: Web框架（AIGC服务）
- PyMySQL: MySQL数据库驱动（AIGC服务使用）
- LangChain: RAG框架
- ChromaDB: 向量数据库
- Pillow (PIL): 图像处理
- BeautifulSoup4: HTML解析
- Requests: HTTP请求

前端技术

- Vue.js 3: 前端框架（Composition API）
- Vite: 构建工具
- Vue Router 4: 路由管理
- Fetch API: HTTP客户端

数据库

- MySQL 5.7+ / MySQL 8.0+: 关系型数据库
- 字符集: utf8mb4（支持emoji等特殊字符）
- 连接方式: JDBC（Java后端），PyMySQL（Python服务）

AI模型（需要Python服务）

- 文字生成: 通义千问 (Tongyi) / OpenAI GPT
- 图像生成: 火山引擎 Seedream (Huoshan) / 通义千问
- 图像理解: 通义千问多模态模型

================================================================================

数据库设计

核心表结构

1. 用户表 (users)

CREATE TABLE users (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  account VARCHAR(20) UNIQUE NOT NULL COMMENT '用户账号（8-10位数字，系统自动生成，永久不可修改）',
  password_hash VARCHAR(255) NOT NULL COMMENT '加密后的密码（SHA-256）',
  role ENUM('普通用户', '管理员') DEFAULT '普通用户',
  nickname VARCHAR(100) COMMENT '用户昵称（可修改）',
  avatar_path VARCHAR(255) DEFAULT '/default.jpg',
  security_question VARCHAR(255) COMMENT '自定义安全问题',
  security_answer_hash VARCHAR(255) COMMENT '安全问题答案哈希',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

设计要点：
- `account` 字段：系统自动生成的8-10位数字账号，唯一且不可修改
- `password_hash`：使用SHA-256单向哈希加密
- `role`：枚举类型，区分普通用户和管理员

2. 文化资源表 (cultural_resources)

存储爬虫抓取或用户上传的原始文化素材。

关键字段：
- `title`: 节日名称（英文）
- `content_feature_data`: JSON格式的文本内容和元数据
- `upload_user_id`: 上传用户ID（外键，级联删除时设为NULL）

3. 文化实体表 (cultural_entities)

存储从资源中提取的结构化实体信息。

关键字段：
- `entity_name`: 实体名称（中文，已清洗）
- `entity_type`: ENUM类型（'人物', '作品', '事件', '地点', '其他'）
- `description`: 详细描述（已清洗）

4. 爬虫图片表 (crawled_images)

存储爬虫抓取的图片元数据。

关键字段：
- `file_name`: 文件名（如 "8.jpg" 或 "8-1.jpg"）
- `storage_path`: 存储路径（如 "crawled_images/8.jpg"）
- `resource_id`: 关联的文化资源ID（外键，可为NULL）
- `entity_id`: 关联的文化实体ID（外键，可为NULL）
- `festival_name`: 节日名称（中文，用于快速查询）
- `tags`: JSON数组格式的标签（已清洗，移除纯数字等无关信息）

关联关系：
- 如果图片属于某个文字资源，`resource_id` 和 `entity_id` 不为NULL
- 如果图片是独立抓取的，`resource_id` 和 `entity_id` 为NULL

5. 用户访问日志表 (user_access_logs)

记录所有用户访问行为。

关键字段：
- `user_id`: 用户ID（可为NULL，表示未登录访问）
- `access_type`: 访问类型（'page_view', 'aigc_text', 'aigc_image', 'search', 'upload'等）
- `access_detail`: JSON格式的访问详情
- `ip_address`: 用户IP地址
- `user_agent`: 用户代理字符串

数据关联关系

cultural_resources (1) ──< (N) crawled_images
cultural_entities (1) ──< (N) crawled_images
users (1) ──< (N) qa_sessions
qa_sessions (1) ──< (N) qa_messages
users (1) ──< (N) user_access_logs

用户表字段说明

users表包含以下关键字段：
- `account`: 用户账号（8-10位数字，系统自动生成，永久不可修改）
- `password_hash`: 密码哈希值（SHA-256）
- `role`: 用户角色（'普通用户' 或 '管理员'）
- `nickname`: 用户昵称（可选，可修改，最多100字符）
- `signature`: 个人签名（可选，可修改，最多500字符）
- `avatar_path`: 头像路径（默认：'/default.jpg'）
- `security_question`: 自定义安全问题（用于找回密码）
- `security_answer_hash`: 安全问题答案哈希值（SHA-256）

================================================================================

数据库连接方式

Java后端 - JDBC连接

Java后端使用JDBC连接MySQL数据库，配置在 `DataSourceConfig.java` 中：

```java
@Configuration
public class DataSourceConfig {
    @Bean
    public DataSource dataSource() {
        DriverManagerDataSource dataSource = new DriverManagerDataSource();
        dataSource.setDriverClassName("com.mysql.cj.jdbc.Driver");
        dataSource.setUrl("jdbc:mysql://127.0.0.1:3306/java_project?...");
        dataSource.setUsername("root");
        dataSource.setPassword("password");
        return dataSource;
    }
    
    @Bean
    public JdbcTemplate jdbcTemplate(DataSource dataSource) {
        return new JdbcTemplate(dataSource);
    }
}
```

数据库配置在 `application.yml` 中：
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/java_project?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: M17382930994c@
```

使用JdbcTemplate进行数据库操作：
```java
@Repository
public class UserDao {
    @Autowired
    private JdbcTemplate jdbcTemplate;
    
    public Optional<User> findById(Long id) {
        String sql = "SELECT * FROM users WHERE id = ?";
        List<User> users = jdbcTemplate.query(sql, 
                new BeanPropertyRowMapper<>(User.class), id);
        return users.isEmpty() ? Optional.empty() : Optional.of(users.get(0));
    }
}
```

Python后端 - PyMySQL连接（爬虫和AIGC使用）

Python模块（爬虫、AIGC）仍使用PyMySQL连接数据库：

```python
import pymysql
conn = pymysql.connect(
    host="127.0.0.1",
    port=3306,
    user="root",
    password="password",
    database="java_project",
    charset="utf8mb4",
    cursorclass=DictCursor
)
```

核心功能实现

1. 用户认证系统（Java后端）

账号生成逻辑（Java实现）

```java
public static String generateRandomAccount() {
    Random random = new Random();
    int length = random.nextInt(3) + 8; // 8-10位
    int firstDigit = random.nextInt(9) + 1; // 1-9
    StringBuilder account = new StringBuilder();
    account.append(firstDigit);
    for (int i = 1; i < length; i++) {
        account.append(random.nextInt(10));
    }
    return account.toString();
}
```

特点：
- 自动生成唯一账号
- 最多尝试100次确保唯一性
- 账号一旦生成永久不可修改

密码加密（Java实现）

使用SHA-256单向哈希算法：

```java
public static String hashPassword(String password) {
    MessageDigest digest = MessageDigest.getInstance("SHA-256");
    byte[] hash = digest.digest(password.getBytes());
    StringBuilder hexString = new StringBuilder();
    for (byte b : hash) {
        String hex = Integer.toHexString(0xff & b);
        if (hex.length() == 1) {
            hexString.append('0');
        }
        hexString.append(hex);
    }
    return hexString.toString();
}
```

2. 数据爬取系统

图片命名规则

单图片资源：
- 格式：`{序号}.jpg`（如：`8.jpg`）

多图片资源：
- 格式：`{基准序号}-{子序号}.jpg`（如：`8-1.jpg`, `8-2.jpg`, `8-3.jpg`）
- 规则：如果检测到资源有多张图片（>=2），所有图片都使用分组命名

数据清洗逻辑

中华民族资源库数据清洗：
- 移除分页信息（"上一页 下一页 总记录数"等）
- 移除日期格式（如"中元节2019-08-14"）
- 移除重复的节日名称行
- 移除纯节日名称列表项

维基百科数据清洗：
- 移除引用标记（如`[1]`、`[注 1]`）
- 移除编辑链接标记（如`[编辑]`）
- 移除分类信息

Tags清洗：
- 移除纯数字标签（URL路径中的ID）
- 移除空字符串和太短的标签（<2字符）
- 保留有意义的标签（节日名称、文化关键词等）

3. AIGC系统

RAG检索增强生成

工作流程：
1. 用户输入问题或上传图片
2. 如果上传图片，先进行图像理解
3. 从向量数据库中检索相关文化资源
4. 将检索结果作为上下文，调用AI模型生成回答
5. 保存对话记录到数据库

延迟加载优化：
- RAG系统和ImageAIGC系统采用延迟加载
- 只在用户首次使用时才初始化
- 显著减少系统启动时间（从19194ms降至数秒）

图像生成

工作流程：
1. 用户输入提示词或上传参考图片
2. 如果没有提示词，自动生成故事
3. 调用图像生成API（Huoshan模型）
4. 保存生成的图片到本地和数据库

4. 数据关联系统

文字资源与图片的关联：
- 爬虫先保存文字资源，获取`resource_id`和`entity_id`
- 保存图片时，将`resource_id`和`entity_id`写入`crawled_images`表
- 如果只有图片没有文字，图片的`resource_id`和`entity_id`为NULL

查询优化：
- 通过`festival_name`字段快速查询某个节日的所有图片
- 通过`resource_id`查询某个文字资源的所有图片
- 通过`entity_id`查询某个文化实体的所有图片

5. 前端设置页面系统

设置页面采用列表式布局（类似QQ设置），包含以下功能：

账号显示：
- 仅显示，不可修改
- 显示用户的8-10位数字账号

昵称管理：
- 可点击进入修改界面
- 下方显示当前昵称
- 支持最多100个字符
- API: POST /api/auth/update-nickname

个人签名管理：
- 可点击进入设置界面
- 下方显示当前签名（未设置时显示"未设置"）
- 支持最多500个字符
- 使用textarea输入框，支持多行文本
- 显示字符计数（已输入/500）
- API: POST /api/auth/update-signature

密码管理：
- 可点击进入修改界面
- 支持两种验证方式：
  - 原密码验证
  - 二级密码验证（如果已设置安全问题）
- API: POST /api/auth/change-password

二级问题管理：
- 可点击进入设置界面
- 如果已设置，需要先验证原答案
- 支持设置新问题和新答案
- API: POST /api/auth/change-security-question

头像管理：
- 方式一：通过设置列表进入
- 方式二：直接点击页面右上角的用户头像
- 支持上传新头像（自动裁剪到200x200像素）
- 支持使用默认头像
- API: POST /api/auth/change-avatar

账号注销：
- 位于设置列表最底部
- 红色文字，表示危险操作
- 需要输入密码确认
- 注销后账号永久删除，无法恢复
- API: POST /api/auth/delete-account

技术实现：
- 使用Vue 3 Composition API
- 使用ref管理状态（settingsTab, fromAvatarClick等）
- 条件渲染显示不同的设置面板
- 使用localStorage保存用户信息
- 所有API调用都包含X-User-Id请求头

================================================================================

API接口文档

认证相关

POST /api/auth/register
用户注册（自动生成账号）

请求体：
{
  "password": "用户密码",
  "nickname": "用户昵称（可选）",
  "security_question": "安全问题（可选）",
  "security_answer": "安全问题答案（可选）"
}

响应：
{
  "success": true,
  "message": "注册成功！您的账号：12345678，请妥善保管，可直接登录。",
  "user_info": {
    "id": 1,
    "account": "12345678",
    "nickname": "用户昵称",
    "role": "普通用户"
  }
}

POST /api/auth/login
用户登录

请求体：
{
  "account": "12345678",
  "password": "用户密码"
}

POST /api/auth/update-nickname
修改用户昵称

请求头：
- `X-User-Id`: 用户ID

请求体：
{
  "nickname": "新昵称"
}

响应：
{
  "success": true,
  "message": "昵称修改成功",
  "user_info": {
    "id": 1,
    "account": "123456789",
    "nickname": "新昵称",
    "signature": "个人签名",
    "avatar_path": "/default.jpg",
    "role": "普通用户"
  }
}

POST /api/auth/update-signature
修改个人签名

请求头：
- `X-User-Id`: 用户ID

请求体：
{
  "signature": "个人签名内容（最多500字符）"
}

响应：
{
  "success": true,
  "message": "个人签名修改成功",
  "user_info": {
    "id": 1,
    "account": "123456789",
    "nickname": "用户昵称",
    "signature": "个人签名内容",
    "avatar_path": "/default.jpg",
    "role": "普通用户"
  }
}

资源相关

GET /api/home/resources
获取首页资源列表

查询参数：
- `page`: 页码（默认1）
- `page_size`: 每页数量（默认12）

响应：
{
  "success": true,
  "resources": [
    {
      "id": 1,
      "festival_name": "中元节",
      "description": "资源描述...",
      "image_url": "/api/images/crawled/8.jpg"
    }
  ],
  "total": 100,
  "page": 1,
  "page_size": 12
}

GET /api/resource/detail
获取资源详情

查询参数：
- `festival_name`: 节日名称（必需）

响应：
{
  "success": true,
  "resource": {
    "festival_name": "中元节",
    "description": "详细描述...",
    "images": [
      {
        "url": "/api/images/crawled/8-1.jpg",
        "index": 1
      }
    ]
  }
}

AIGC相关

POST /api/aigc/chat
AIGC聊天接口

请求头：
- `X-User-Id`: 用户ID

请求体（multipart/form-data）：
- `mode`: 'text' 或 'image'
- `query`: 用户输入（可选）
- `images`: 图片文件（可选）
- `session_id`: 会话ID（可选）

响应：Server-Sent Events (SSE) 流式输出

管理员相关

GET /api/admin/dashboard/statistics
获取数据大屏统计信息

请求头：
- `X-User-Id`: 用户ID（必须是管理员）

响应：
{
  "success": true,
  "statistics": {
    "total_users": 100,
    "today_users": 10,
    "total_text_aigc_users": 50,
    "today_text_aigc_users": 5,
    "total_image_aigc_users": 30,
    "today_image_aigc_users": 3,
    "trend_data": [
      {
        "date": "2024-01-15",
        "visits": 10,
        "text_aigc": 5,
        "image_aigc": 3
      }
    ]
  }
}

POST /api/admin/log-access
记录用户访问日志

请求体：
{
  "user_id": 1,
  "access_type": "page_view",
  "access_detail": {
    "page": "/dashboard"
  }
}

================================================================================

代码规范与最佳实践

Java代码规范

1. 命名规范：
   - 类名：大驼峰（`AuthController`）
   - 方法名：小驼峰（`getUserById`）
   - 变量名：小驼峰（`userId`）
   - 常量：全大写下划线（`MAX_RETRY_COUNT`）
   - 包名：小写，点分隔（`com.cultural.service`）

2. 架构规范：
   - Controller层：处理HTTP请求，参数验证
   - Service层：业务逻辑处理
   - DAO层：数据访问，使用JdbcTemplate
   - Entity层：实体类，对应数据库表

3. 错误处理：
   - 使用try-catch捕获异常
   - 记录详细的错误日志
   - 返回用户友好的错误消息
   - 使用ResponseEntity包装响应

4. 数据库操作：
   - 使用JdbcTemplate进行数据库操作
   - 使用@Transactional管理事务
   - 使用预编译语句防止SQL注入

5. 代码复用：
   - 避免重复代码
   - 提取公共方法
   - 使用配置类管理配置

Python代码规范（爬虫和AIGC模块）

1. 命名规范：
   - 类名：大驼峰（`AuthSystem`）
   - 函数名：小写下划线（`get_user_by_id`）
   - 私有方法：单下划线前缀（`_hash_password`）
   - 常量：全大写下划线（`MAX_RETRY_COUNT`）

2. 错误处理：
   - 使用try-except捕获异常
   - 记录详细的错误日志
   - 返回用户友好的错误消息

3. 数据库操作：
   - 使用上下文管理器（`with conn.cursor()`）
   - 及时提交或回滚事务
   - 关闭数据库连接

4. 代码复用：
   - 避免重复代码
   - 提取公共函数
   - 使用配置常量而非硬编码

Vue.js代码规范

1. 组件结构：
   - 使用Composition API
   - `<script setup>` 语法
   - 单一职责原则

2. 状态管理：
   - 使用`ref`和`reactive`管理状态
   - 避免过度使用全局状态
   - 合理使用`computed`和`watch`

3. API调用：
   - 统一错误处理
   - 显示加载状态
   - 用户友好的错误提示

已修复的代码质量问题

1. 硬编码问题：
   - ✅ 移除硬编码的数据库密码
   - ✅ 使用环境变量配置
   - ✅ 提取魔法数字为常量

2. 重复代码：
   - ✅ 统一数据库连接管理（`db_connection.py`）
   - ✅ 统一日志记录（`user_logging.py`）
   - ✅ 统一错误处理模式

3. 命名不一致：
   - ✅ 统一使用`account`替代`username`
   - ✅ 统一字段命名规范

4. 性能优化：
   - ✅ 延迟加载AI模型（减少启动时间）
   - ✅ 数据库索引优化
   - ✅ 图片懒加载

================================================================================

部署与运维

环境要求

- Java 17 或更高版本
- Maven 3.6 或更高版本
- MySQL 5.7+ / MySQL 8.0+
- Node.js 16+（前端）
- Python 3.7+（可选，用于爬虫和AIGC功能）
- 至少2GB内存

部署步骤

1. 安装Java和Maven：
   - 下载并安装JDK 17
   - 下载并安装Maven 3.6+
   - 配置JAVA_HOME和MAVEN_HOME环境变量

2. 安装其他依赖：
   python install_dependencies.py
   （这会安装Python和Node.js依赖）

3. 初始化数据库：
   python database_files/run_init_schema.py

4. 配置环境变量：
   创建`.env`文件，配置数据库和API密钥
   
   数据库配置在 `backend/src/main/resources/application.yml` 中：
   ```yaml
   spring:
     datasource:
       url: jdbc:mysql://127.0.0.1:3306/java_project?...
       username: root
       password: your_password
   ```

5. 启动Java后端：
   cd backend
   mvn spring-boot:run
   
   或使用启动脚本：
   # Windows
   cd backend
   start_backend.bat
   
   # Linux/Mac
   cd backend
   chmod +x start_backend.sh
   ./start_backend.sh

6. 启动前端：
   cd FrontEnd
   npm run dev

7. （可选）启动Python AIGC服务（如果需要AI功能）：
   python AIGC/aigc_api_server.py

监控与日志

- 访问日志：存储在`user_access_logs`表
- 错误日志：
  - Java后端：控制台输出，可通过logback配置日志文件
  - Python服务：控制台输出
- 数据统计：管理员数据大屏
- 日志级别配置：在`application.yml`中配置

备份策略

1. 数据库备份：
   - 定期备份MySQL数据库
   - 建议每日备份

2. 文件备份：
   - `crawled_images/`：爬虫图片
   - `AIGC_graph/`：AIGC生成图片
   - `FrontEnd/public/`：用户头像

性能优化建议

1. 数据库优化：
   - 定期清理历史日志数据
   - 优化慢查询
   - 使用连接池

2. 缓存策略：
   - 考虑使用Redis缓存热点数据
   - 静态资源使用CDN

3. 图片优化：
   - 压缩大图片
   - 使用WebP格式
   - 实现图片懒加载

================================================================================

常见问题排查

1. 数据库连接失败

检查项：
- MySQL服务是否运行
- `backend/src/main/resources/application.yml`中的数据库配置是否正确
- 数据库用户权限
- JDBC驱动是否正确加载

2. API请求失败

检查项：
- Java后端服务是否运行（端口8000）
  - 检查：`cd backend && mvn spring-boot:run`
  - 查看控制台是否有启动错误
- 前端代理配置是否正确（`FrontEnd/vite.config.js`）
- 查看后端日志

3. 图片无法显示

检查项：
- 图片文件是否存在
- 文件路径是否正确
- WebConfig中的静态资源路径配置是否正确
- 文件权限设置

4. AIGC功能异常

检查项：
- Python AIGC服务是否运行（如果使用AI功能）
- AI模型API密钥配置
- 网络连接是否正常
- 查看错误日志

5. Maven依赖下载失败

检查项：
- Maven配置是否正确
- 网络连接是否正常
- 可以配置Maven镜像加速下载

================================================================================

版本历史

v2.0.0 (当前版本) - 2025年12月20日

主要更新：
- ✅ 后端从Python Flask迁移到Java Spring Boot
- ✅ 数据库连接从PyMySQL改为JDBC
- ✅ 采用标准的MVC架构（Controller-Service-DAO）
- ✅ 改进代码组织和可维护性

主要特性：
- 用户注册/登录（自动生成账号）
- 文化资源爬取与清洗
- AIGC文字/图片生成（Python服务）
- 数据大屏统计
- 评论与通知系统

技术改进：
- 使用Java Spring Boot提供更稳定的后端服务
- JDBC连接提供更好的数据库性能
- 标准化的代码架构便于维护和扩展
- 延迟加载优化启动速度（Python AIGC服务）
- 数据关联优化
- 代码质量提升

v1.0.0 (历史版本)

主要特性：
- 用户注册/登录（自动生成账号）
- 文化资源爬取与清洗
- AIGC文字/图片生成
- 数据大屏统计
- 评论与通知系统

技术栈：Python Flask + PyMySQL

================================================================================

许可证

本项目遵循项目主许可证。

