# 需求优化专业提示词

## 需求1: AI检索功能修复

**提示词:**
```
作为AI系统架构师，请检查并修复AI检索功能的实现问题。需要确保：
1. 向量检索（Vector Retrieval）功能正常工作，检查Chroma向量库的连接、查询和返回结果
2. 数据库检索（Database Retrieval）功能正常工作，检查SQL查询逻辑、表关联和结果返回
3. 检索结果正确传递给RAG系统，确保检索到的资源ID能够被正确提取和保存
4. 检查rag_base.py中的_call_retriever、query_database等方法实现
5. 验证检索结果是否包含必要的metadata（如resource_id、entity_id等）
6. 添加详细的错误日志，便于定位问题
```

## 需求2: 图片AIGC速度优化

**提示词:**
```
作为性能优化专家，请分析并优化图片AIGC功能的生成速度。需要：
1. 分析当前性能瓶颈：检查API调用、图片下载、本地保存等环节的耗时
2. 优化策略：
   - 实现异步/并发处理（如使用asyncio或threading）
   - 优化图片下载和保存流程（减少I/O等待）
   - 缓存重复的检索结果
   - 优化提示词生成流程（减少不必要的LLM调用）
3. 添加性能监控：记录各环节耗时，识别瓶颈
4. 考虑使用更快的图片生成模型或调整模型参数
5. 优化连环画生成：如果生成多张图片，考虑并行生成而非串行
```

## 需求3: AI处理过程用户提示

**提示词:**
```
作为UX设计师和全栈开发者，请为AI处理过程添加实时用户提示功能。需要：
1. 实现进度反馈机制：
   - 在文字AIGC中：显示"正在检索相关资源..."、"正在生成回答..."等提示
   - 在图片AIGC中：显示"正在理解图片..."、"正在生成故事..."、"正在生成第X张图片..."等提示
2. 使用Server-Sent Events (SSE)或WebSocket实现实时推送
3. 前端显示加载动画和进度文本，让用户知道系统正在工作
4. 处理异常情况：显示"处理中遇到问题，正在重试..."等提示
5. 确保提示信息清晰、友好，避免技术术语
```

## 需求4: AI头像更换

**提示词:**
```
作为UI设计师，请将AI头像更换为更美观且具有科技感的头像。需要：
1. 设计或选择一个具有科技感的AI头像（可以是抽象几何图形、未来感设计等）
2. 头像应体现AI助手的特性：智能、现代、友好
3. 确保头像在不同尺寸下清晰显示
4. 更新前端组件中的头像路径或base64编码
5. 检查所有使用AI头像的地方（聊天界面、用户界面等）并统一更新
```

## 需求5: 图片AIGC多图生成逻辑修复

**提示词:**
```
作为AI产品逻辑专家，请修复图片AIGC的多图生成逻辑。需要：
1. 修复误判问题：只在用户明确要求生成连环画/漫画/系列图时才生成多张图片
2. 改进检测逻辑：优化_is_comic_request方法，避免误判普通单图请求
3. 确保连贯性：
   - 故事生成时确保场景之间的逻辑连贯
   - 图片生成时保持人物形象、环境、风格的一致性
   - 文字说明要承上启下，形成完整故事链条
4. 优化prompt模板：在story_prompt_template和scene_prompt_template中强调连贯性要求
5. 添加连贯性检查：在生成每个场景时，参考前一个场景的信息
```

## 需求6: 图文互搜功能修复

**提示词:**
```
作为全栈开发工程师，请修复图文互搜功能的多个问题。需要：
1. 修复404错误：
   - 检查资源详情API的路由和参数传递
   - 确保资源ID正确传递和解析
   - 修复图片URL构建逻辑
2. 实现图片内容理解：
   - 使用视觉模型（如GPT-4 Vision）将上传的图片转化为文字描述
   - 将图片描述转化为向量
   - 使用文字描述与数据库中的文字内容进行匹配
3. 隐藏图片路径信息：
   - 删除所有返回给前端的图片路径相关字段
   - 只返回处理后的图片URL（如/api/images/xxx）
   - 检查所有相关API接口，确保不泄露文件系统路径
4. 优化搜索逻辑：确保图片搜索和文本搜索都能正常工作
```

## 需求7: 文本资源上传功能修复

**提示词:**
```
作为功能开发工程师，请修复文本资源上传功能。需要：
1. 支持直接输入文本：
   - 当用户选择资源类型为"文本"时，允许直接输入文本内容
   - 不需要强制上传文件
2. 修改前端组件：在ResourceUpload.vue中添加文本输入框
3. 修改后端API：支持接收文本内容（而非仅文件）
4. 确保直接输入的文本也能正常提交AI标注
5. 保持文件上传功能仍然可用（两种方式都支持）
```

## 需求8: AI标注暂停功能修复

**提示词:**
```
作为系统管理员功能开发工程师，请修复AI标注暂停功能。需要：
1. 修复"无效的用户ID"错误：
   - 检查用户ID的获取逻辑（header、body等）
   - 确保管理员用户ID能够正确识别
   - 检查数据库查询逻辑
2. 检查AI标注功能实现：
   - 验证auto_annotation.py中的服务是否正常运行
   - 检查标注任务的创建和更新逻辑
   - 确保标注任务状态正确更新
3. 修复权限检查：确保管理员和超级管理员都能正常使用暂停功能
4. 添加详细的错误日志，便于定位问题
5. 测试暂停功能：确保暂停后任务状态正确更新，且不会继续处理
```

## 需求9: retrieval_id字段填充修复

**提示词:**
```
作为数据持久化工程师，请修复retrieval_id字段的填充功能。需要：
1. 检查检索结果提取逻辑：
   - 确保从RAG系统的ask方法返回结果中正确提取resource_id
   - 检查retrieved_resources结构，确保包含必要的ID信息
2. 修复保存逻辑：
   - 确保在save_aigc_message_to_db中正确保存retrieval_id
   - 检查字段是否存在（has_retrieval_id_field检查）
   - 确保ID列表格式正确（英文逗号分隔）
3. 验证数据持久化：
   - 检查qa_messages表中retrieval_id字段是否有值
   - 确保退出对话或发送下一条消息后，之前的retrieval_id仍然保存
4. 修复历史消息加载：确保从数据库加载历史消息时，retrieval_id能够正确读取和显示
```

## 需求10: 图片理解功能检查与优化

**提示词:**
```
作为AI视觉理解专家，请检查并优化所有与图片理解相关的功能。需要：
1. 检查所有图片理解功能：
   - 图文互搜中的图片理解
   - AIGC中的图片理解（文字和图片模式）
   - 其他涉及图片分析的场景
2. 确保真正理解图片语义：
   - 使用视觉模型（如GPT-4 Vision、Claude Vision等）进行深度理解
   - 不仅识别图片中的物体，还要理解文化内涵、传统元素等
   - 生成详细的文字描述，包含文化相关元素
3. 优化理解质量：
   - 改进prompt，要求模型重点关注传统节日、文化元素
   - 确保描述准确、详细，能够用于后续检索和生成
4. 统一实现方式：确保所有图片理解功能使用相同的视觉模型和prompt策略
5. 添加验证机制：检查图片理解结果的质量，必要时进行后处理
```

## 需求11: 爬虫代码优化

**提示词:**
```
作为数据工程师，请优化爬虫代码的数据清洗和去重逻辑。需要：
1. 实现数据清洗：
   - 清洗文本内容：去除HTML标签、特殊字符、无关信息
   - 清洗图片：去除重复图片、无效图片、非文化相关图片
   - 标准化数据格式：统一日期格式、文本编码等
2. 实现去重机制：
   - 检查图片是否已存在（通过URL、文件hash等）
   - 避免重复保存相同或相似的图片
   - 检查文本资源是否重复（通过内容hash）
3. 验证清洗逻辑：
   - 从数据库随机抽取30条数据（分6批，每批5条）
   - 检查每条资源的所有相关信息（文字表、图片表等）
   - 验证数据质量：完整性、准确性、去重效果
4. 确保爬取资源符合要求：
   - 只爬取中国传统节日（包括少数民族传统节日）
   - 过滤掉非节日相关内容
   - 验证节日名称和内容的准确性
5. 优化爬虫性能：
   - 减少不必要的数据库查询
   - 优化图片下载和保存流程
   - 添加进度显示和错误处理
```

